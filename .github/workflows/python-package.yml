  
name: Python package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: build executable
    runs-on: windows-latest
    steps:
    - name: download git file
      uses: actions/checkout@v2
    - name: config-repo validatiion
      run: |
        pip install yamale
        import yamale
        schema = yamale.make_schema('pycatmanadapter\schema\schemaConfig.yaml')
        schema1 = yamale.make_schema('pycatmanadapter\schema\schemaConfig1.yaml')
        data = yamale.make_data('pycatmanadapter\config_repo\pyadapter\local\master\resources\locationConfig.yaml')
        yamale.validate(schema,data)
